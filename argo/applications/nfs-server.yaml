apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nfs-server
  namespace: argocd
spec:
  destination:
    namespace: nfs-server
    server: https://kubernetes.default.svc
  project: default
  source:
    path: manifests/applications/nfs-server
    repoURL: https://github.com/xelab04/hopeops.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      enabled: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideAppy=true
