apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: towonel
  name: towonel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: towonel
  strategy: {}
  template:
    metadata:
      labels:
        app: towonel
    spec:
      containers:
      - image: git.erwanleboucher.dev/eleboucher/towonel-agent:latest
        name: towonel-agent
        resources: {}
        env:
          - name: TOWONEL_INVITE_TOKEN
            valueFrom:
              secretKeyRef:
                name: towonel-inv-key
                key: TOWONEL_SELF_TOKEN
          - name: TOWONEL_AGENT_SERVICES
            valueFrom:
              configMapKeyRef:
                name: towonel-hostnames
                key: TOWONEL_SELF_SERVICES

status: {}
