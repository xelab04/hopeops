# apiVersion: traefik.io/v1alpha1
# kind: IngressRoute
# metadata:
#   name: notify-route
# spec:
#   entryPoints:
#   - websecure
#   - web
#   routes:
#   - kind: Rule
#     match: Host(`notify.alexbissessur.dev`)
#     services:
#     - name: ntfy
#       port: 80
#   tls:
#     certResolver: le


apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gts
  annotations:
    cert-manager.io/cluster-issuer: "prod-issuer"
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - notify.alexbissessur.dev
    secretName: notify-tls
  rules:
  - host: notify.alexbissessur.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ntfy
            port:
              number: 80
